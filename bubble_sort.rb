def bubble_sort_by(arr)
    arr.length.times do
        arr.each_with_index do | x, i|
            left = arr[i]
            right = arr[i + 1]
            
                
        end
    end
end
    
